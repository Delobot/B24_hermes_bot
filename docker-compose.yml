version: '3.8'

services:
  hermes-bot:
    build: .
    container_name: hermes-bot
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
